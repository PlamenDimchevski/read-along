import { SeriesForm } from '../form';

export default function AddBooksSeries() {
   return (
      <SeriesForm>
         <h2 className="text-lg font-bold">Add new book series</h2>
      </SeriesForm>
   );
}
